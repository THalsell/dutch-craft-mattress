import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partner With Us | Dutch Craft Mattress",
  description: "Interested in carrying Dutch Craft mattresses? Contact us to become an authorized retailer and partner.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
